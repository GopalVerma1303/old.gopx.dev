---
title: Showcase
description: Project Gallery - A curated showcase of my works, collaborations and job positions 📺
---

import { Cards } from 'nextra/components'
import {WORKS, COLLABORATIONS, FIRE_WORKS, JOBS, CONTRIBUTIONS} from '@/content/showcase'
import Image from 'next/image'

export function ShowcaseHeader() {
  return (
    <div className="max-w-screen-lg mx-auto pt-4 pb-8 mb-16 border-b border-gray-400 border-opacity-20">
      <h1>
        <span className="font-bold leading-tight lg:text-5xl">Showcase</span>
      </h1>
      <p className="italic text-center text-gray-500 dark:text-gray-400 font-space-grotesk">
        Explore my showcased projects, and collaborations each demonstrating my skills and creativity.
      </p>
    </div>
  )
}

<ShowcaseHeader />

<p className="text-xl font-bold mb-4">🔥ing </p>

<div class="grid grid-cols-1 gap-4 sm:grid-cols-2 lg:grid-cols-3 border-b border-gray-400 border-opacity-20 mb-16 pb-8">
  {
    FIRE_WORKS.map((work) => (
    <Cards>
      <ShowcaseCard title={work.title} href={work.href} >
        <Image src={work.img} width={500} height={500} />
      </ShowcaseCard>
    </Cards> 
    ))
  }
</div>


<p className="text-xl font-bold mb-4">Works</p>

<div class="grid grid-cols-1 gap-4 sm:grid-cols-2 lg:grid-cols-3 border-b border-gray-400 border-opacity-20 mb-16 pb-8">
  {
    WORKS.map((work) => (
    <Cards>
      <ShowcaseCard title={work.title} href={work.href} >
        <Image src={work.img} width={500} height={500} />
      </ShowcaseCard>
    </Cards> 
    ))
  }
</div>

<p className="text-xl font-bold mb-4">Collaborations</p>

<div class="grid grid-cols-1 gap-4 sm:grid-cols-2 lg:grid-cols-3 border-b border-gray-400 border-opacity-20 mb-16 pb-8">
  {
    COLLABORATIONS.map((work) => (
    <Cards>
      <ShowcaseCard title={work.title} href={work.href} >
        <Image src={work.img} width={500} height={500} />
      </ShowcaseCard>
    </Cards> 
   ))
  }
</div>

<p className="text-xl font-bold mb-4">Contributions</p>

<div class="grid grid-cols-1 gap-4 sm:grid-cols-2 lg:grid-cols-3 border-b border-gray-400 border-opacity-20 mb-16 pb-8">
  {
    CONTRIBUTIONS.map((work) => (
    <Cards>
      <ShowcaseCard title={work.title} href={work.href} >
        <Image src={work.img} width={500} height={500} />
      </ShowcaseCard>
    </Cards> 
   ))
  }
</div>


<p className="text-xl font-bold mb-4">Jobs</p>

<div class="grid grid-cols-1 gap-4 sm:grid-cols-2 lg:grid-cols-3 border-b border-gray-400 border-opacity-20 mb-16 pb-8">
  {
    JOBS.map((work) => (
    <Cards>
      <ShowcaseCard title={work.title} href={work.href} >
        <Image src={work.img} width={500} height={500} />
      </ShowcaseCard>
    </Cards> 
   ))
  }
</div>


export const ShowcaseCard = Object.assign(
  // Copy card component and add default props
  Cards.Card.bind(),
  {
    displayName: 'ShowcaseCard',
    defaultProps: {
      image: true,
      arrow: true,
    }
  }
)
